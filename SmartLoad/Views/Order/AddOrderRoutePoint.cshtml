@* @model SmartLoad.Models.OrderRoutePoint

@{
    ViewData["Title"] = "Добавить связь между заказом и точкой маршрута";
    var orderId = ViewBag.OrderId;
}

<h1>Добавить связь между заказом и точкой маршрута для заказа: @orderId</h1>

<form asp-action="AddOrderRoutePoint" asp-controller="Home" method="post">
    <input type="hidden" asp-for="OrderId" value="@orderId" />

    <div class="form-group">
        <label asp-for="RoutePointId" class="control-label"></label>
        <select asp-for="RoutePointId" class="form-control" asp-items="ViewBag.RoutePointId">
            <option value="">Выберите точку маршрута</option>
        </select>
        <span asp-validation-for="RoutePointId" class="text-danger"></span>
    </div>

    <div class="form-group mt-3">
        <button type="submit" class="btn btn-primary">Сохранить</button>
        <a href="@Url.Action("OrderRoutePoints", "Order", new { orderId })" class="btn btn-secondary">Отмена</a>
    </div>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
} *@