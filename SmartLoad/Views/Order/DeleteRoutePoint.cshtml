@model SmartLoad.Models.RoutePoint

@{
    ViewData["Title"] = "Удалить точку разгрузки";
}

<h1>Удалить точку разгрузки</h1>

<h3>Вы уверены, что хотите удалить эту точку разгрузки?</h3>
<div>
    <h4>RoutePoint</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.UnloadingDate)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.UnloadingDate)
        </dd>
    </dl>

    <form asp-action="DeleteRoutePoint">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Удалить" class="btn btn-danger" /> |
        <a asp-action="RoutePoints">Назад к списку</a>
    </form>
</div>
